<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Playlist</title>
</head>
<body>
    <header>
        <h1>Playlist</h1>
        <ul>
            <li>
                <a href="/search">
                    Home
                </a>
            </li>
        </ul>
    </header>
    <main>
        <h2>User Playlist</h2>
        <ul>
            <% for( let i = 0; i < link.length; i++ ) { %>
                <li>
                    <img src="<%= link[i].artwork %> " alt="<%= link[i].title %> ">
                    <a href="/search/<%= link[i].id %> ">
                        <section>
                            <%= link[i].title %> <br/>
                            <%= link[i].userHandle %>  <br/>
                            <%= link[i].genre %> <br/>
                            <video  controls name="media">
                                <source src="<%= `${url}/v1/tracks/${link[i].id}/stream?` %>" type="audio/mpeg">
                            </video>
                        </section>
                    </a>
                    <form action="/search" method="POST">
                        <input type="submit" value="Add">                    
                    </form>
                </li>
            <% } %>
        </ul>
    </main>
</body>
</html>